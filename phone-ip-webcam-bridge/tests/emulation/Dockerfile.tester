FROM debian:bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    bash \
    ca-certificates \
    ffmpeg \
    gawk \
    grep \
    sed \
    coreutils \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace/phone-ip-webcam-bridge
