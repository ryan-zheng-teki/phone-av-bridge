# Copy this file to bridge.local.env and adjust values per host.

# MVP protocol is RTSP only.
STREAM_SOURCE_PROTOCOL=rtsp

# Example Android camera source URL (replace with your phone stream URL).
STREAM_SOURCE_URL=rtsp://192.168.1.50:8554/live

# Optional token. Keep empty unless your source pipeline uses tokenized URLs.
STREAM_AUTH_TOKEN=

# linux-v4l2, linux-null-emulator (testing), linux-file-emulator (testing), or macos-extension-adapter
SINK_BACKEND=linux-v4l2

# Linux-only sink target for v4l2loopback
V4L2_DEVICE=/dev/video2

# File output path for linux-file-emulator backend
OUTPUT_FILE=/tmp/phone-av-camera-runtime-output.mp4

# Video output settings
FRAME_SIZE=1280x720
FRAME_RATE=30

# FFmpeg verbosity: quiet, error, warning, info, verbose, debug
FFMPEG_LOGLEVEL=warning

# Optional max runtime seconds (0 means unbounded).
MAX_SECONDS=0
